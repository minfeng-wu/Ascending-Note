checkException:  forced to catch

unchecked: runtime exception and error

hibernate:
2 层 levl cache
1 level: you can not do anything
2 level: you can 


class variable vs instance variable;


***************************8
Design pattern - Singleton
only one instance

how?
private class constructor
private static variable of class itself
public method to return the instance of the variable
但是线程不安全 lazy， data racing

eager：
直接fu值
******************************

driver connection
string sql
execute
close

abstraction vs encapsaulation

session factory heavy,
session: thread safe, 用完就扔掉
